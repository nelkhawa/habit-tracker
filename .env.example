# Local development only — lets `npm run dev` call the Claude API directly
# from the browser. In production, set ANTHROPIC_API_KEY in Vercel instead
# (used by api/analyze.js, so the key never reaches the client).
VITE_ANTHROPIC_API_KEY=sk-ant-...
